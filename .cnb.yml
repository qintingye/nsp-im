main:
  push:
    - stages:
        - name: deploy-to-pages
          image: alpine:latest
          script:
            - mkdir -p /tmp/deploy
            # 👇 这里改成你实际侦察到的文件夹路径
            - cp -r public/* /tmp/deploy/ 
            - ls /tmp/deploy
          artifacts:
            path: /tmp/deploy