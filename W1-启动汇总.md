# NSP-IM W1 启动汇总

> **生成时间**：2026-08-18 02:38
> **PM/Backend/Frontend 3 Agent 已完成评审**

---

## 一、W1 总目标

| 维度 | 目标 |
|---|---|
| **移动端** | 底部 Tab Bar + SVG 响应式 + Bottom Sheet + 全屏详情 |
| **后端** | Git 仓库 + JSON Schema + 1-5 源抓取 + 健康探针 + CI |
| **部署** | 内网强制（ADR-005），公网 Vercel 仅做 PoC |
| **资源** | 1.5 FTE（PM+Frontend+Backend），0 现金 |

---

## 二、W1 5 日任务（Kanban 任务已建）

### Day 1（8/18 已完成 80%）
- ✅ Git 仓库初始化（9 文件 + 2 commits）
- ✅ 3 JSON schema（policies/intelligence/scenes）
- ✅ 1 fetcher 跑通框架（发改委）
- ✅ GitHub Actions CI 配置
- 🟡 30 人设备调研（PM 待办）
- 🟡 内网方案审批（PM 待办）

### Day 2（8/19）
- **FE**：7 Tab 改底栏 + safe-area + 44px 触控区
- **BE**：跑通发改委单源抓取 + 来源追溯
- **验收**：手机无横向溢出 + 生成合法 JSON

### Day 3（8/20）
- **FE**：SVG viewBox 自适应 + 双栏改单栏
- **BE**：超时 + 3 次重试 + 去重 + 健康探针
- **验收**：3 类 SVG 可读 + 失败告警不覆盖旧数据

### Day 4（8/21）
- **FE**：抽屉改 bottom-sheet + 场景详情全屏 + 修重复 init
- **BE**：原子写入 + Git 提交脚本 + 内网部署预览
- **验收**：返回键/关闭/滚动正常 + 部署可回滚

### Day 5（8/22）
- **QA/PM**：iOS Safari + Android Chrome + PC 回归
- **端到端演示**：1 源 → JSON → 部署成功
- **验收**：P0 阻断缺陷为 0

---

## 三、Frontend 改造方案（核心 6 项）

### F1. 底部 Tab Bar（iOS 原生）
- `position: fixed; bottom: 0`
- 56px 高度 + safe-area
- 6 个 tab：网/算/通/管/物/监
- 桌面 ≥768px 自动隐藏（恢复横排）

### F2. SVG 响应式
- `viewBox="0 0 1280 680"` + `preserveAspectRatio="xMidYMid meet"`
- 移动端 <480px：节点精简（只留 Top 2）
- 桌面 ≥1280px：max-width 1280px 居中

### F3. Bottom Sheet（替代右抽屉）
- 底部弹出，最大 85vh
- 圆角 16px + grabber 拖动
- 单击节点触发

### F4. 全屏详情页
- `transform: translateX(100%)` → 0
- 长按节点 600ms 触发
- 左滑 80px 返回

### F5. 性能优化
- 字体本地化（去掉 Google Fonts，省 200-500ms）
- 关键 CSS inline
- 图片懒加载
- Lighthouse 90+ 目标

### F6. PWA 完善
- viewport 三件套（viewport-fit=cover + maximum-scale=5）
- iOS 加主屏引导
- sw.js 升级（v2 缓存）

---

## 四、Backend 方案（核心 5 项）

### B1. 抓取框架（已建）
- `BaseFetcher` 抽象类
- 5 个源：ndrc / nea / csg / sgcc / bjx
- 3 次重试 + 指数退避
- 失败告警 + dead-letter queue

### B2. JSON Schema（已建）
- `policies.schema.json`（37 行）
- `intelligence.schema.json`（每日情报）
- `scenes.schema.json`（6 网协同）

### B3. 健康探针（待建）
- 监控指标：抓取成功率 / 响应时间 / 数据新鲜度
- 告警：飞书 webhook + 邮件
- Fallback：失败不覆盖旧数据

### B4. 原子写入（待建）
- 临时文件 + rename
- 防止半写文件
- Git 单文件 commit

### B5. CI/CD（已配）
- GitHub Actions daily-fetch.yml
- 每日 09:00 自动
- 抓取 → JSON 校验 → commit → push

---

## 五、关键风险（5 项）

| 风险 | 等级 | 应对 |
|---|---|---|
| 内网部署审批延迟 | 🔴 高 | Day 1 启动审批，并行 PoC |
| 政策源网站改版 | 🟡 中 | 多源备份 + Agent 自适应 + 失败告警 |
| iOS 16.4- 用户 | 🟡 中 | 邮件降级 + Web 访问提示 |
| 单文件回归面大 | 🟡 中 | W1 拆细任务 + 逐项验收 |
| Google Fonts 阻塞 | 🟢 低 | Day 5 字体本地化 |

---

## 六、停损门

> **W1 末若以下任一未达，冻结扩源/新功能，只修基础链路**：
- [ ] 单源端到端跑通（抓取 → JSON → 部署）
- [ ] 内网方案审批完成
- [ ] 移动端无横向溢出
- [ ] P0 阻断缺陷 = 0

---

## 七、5 个 Kanban 任务（已建）

| ID | 标题 | 责任人 | 截止 |
|---|---|---|---|
| t_9e9c04ef | W1-D1 基线、ADR与JSON Schema | pm | 8/18 |
| t_dfc87508 | W1-D2 底部Tab与单源抓取 | frontend | 8/19 |
| t_6206d9b1 | W1-D3 SVG适配、重试与健康探针 | frontend | 8/20 |
| t_a8146f44 | W1-D4 全屏详情与内网部署 | backend | 8/21 |
| t_56f5c71b | W1-D5 跨端验收及端到端演示 | pm | 8/22 |

**Board**：`nsp-im`（NSP-IM 六网协同情报平台）
**DB**：`C:\Users\Administrator\AppData\Local\hermes\kanban\boards\nsp-im\kanban.db`

---

## 八、当前进度

| 状态 | 任务 |
|---|---|
| ✅ 已完成 | Git 仓库 / 3 Schema / NDRC fetcher / CI 配置 / 5 Kanban 任务 |
| 🟡 Day 1 收尾 | 30 人设备调研 + 内网审批 |
| 🔵 待启动 | D2 移动端改造 + 单源抓取 |

---

**下一步**：
1. 完成 Day 1 收尾（你 + 领导沟通内网）
2. 启动 D2（移动端 + 单源抓取）
3. 每日 9:00 站会（15 分钟）
4. W1 末（8/22）端到端演示
