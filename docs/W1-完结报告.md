# NSP-IM · W1 完结报告

> **时间**：2026-08-18
> **范围**：W1（8/18）· Backend + Frontend 双轨
> **作者**：Backend Lead (subagent)
> **状态**：W1 后端 DONE ✅

---

## 1. 五天完成情况

| 日 | 主题 | Backend 关键交付 | Frontend 关键交付 | 状态 |
|---|---|---|---|---|
| **D1** | 立项 + 决策 | 决策代决（iOS 占比、内网部署、1.5 FTE、合规审批）| 项目骨架 + Kanban | ✅ D1✓ |
| **D2** | 数据 + UI 打底 | `data/policies.json` demo 7 条 + `data/README.md` | 底部 Tab Bar 移动端 (`11-…移动端.html`) | ✅ D2✓ |
| **D3** | 容错 + 响应式 | `atomic_write` / `health` / `dedup` 3 utils + `tests/test_d3.py` | SVG viewBox 响应式 + 节点精简 + 双截图 | ✅ D3✓ |
| **D4** | 集成 + 部署 | `integrate.py` 编排器 + `main_fetcher.py` + `deploy_precheck.sh` | 抽屉 → bottom-sheet + 场景详情全屏 | ✅ D4✓ |
| **D5** | E2E 验证 | `scripts/e2e.sh` (本文) + 本完结报告 | — | ✅ D5✓ |

```
W1 ▰▰▰▰▰ 100%
```

---

## 2. 关键产出清单（后端）

### 2.1 代码模块 (`src/`)
| 路径 | 行数级 | 用途 |
|---|---|---|
| `src/main_fetcher.py` | ~200 | 每日 09:00 调度入口，支持 `--demo` 离线模式 |
| `src/fetchers/base.py` | 中型 | 抽象基类（异步/同步钩子、错误签名契约）|
| `src/fetchers/ndrc.py` | 中型 | 国家发改委 NDRC 真实 fetcher（PoC 阶段）|
| `src/utils/integrate.py` | 大型 | dedup + atomic + health 三合一编排器 |
| `src/utils/atomic_write.py` | 紧凑 | 失败回滚原子写入（PID 锁 + 临时文件）|
| `src/utils/health.py` | 紧凑 | 探针健康聚合 + stale 标记 |
| `src/utils/dedup.py` | 紧凑 | `id`-first 去重，留 first-wins 语义 |

### 2.2 Schema (`src/schemas/`)
- `policies.schema.json` / `intelligence.schema.json` / `scenes.schema.json`
- Draft-07 全部校验合法 + 业务规则测试（9 用例）

### 2.3 部署与运维
- `scripts/deploy_precheck.sh` — 21 项环境/依赖/单测/JSON schema 自检
- `scripts/e2e.sh` — **W1-D5 新增**：4 阶段端到端验证
- `scripts/git_commit_data.py` — 数据自动入库（hook）
- `.github/workflows/daily-fetch.yml` — CI/CD cron（每日 09:00 UTC+8）

### 2.4 数据与预览
- `data/policies.json` — **12 条** demo 政策（覆盖 5 网 + monitor）
- `data/health.json` — fetcher 探针快照（发改委 + integrate-demo）
- `data/README.md` — 数据使用说明
- `docs/preview/` — `index.html` / `manifest.json` / `sw.js` / `README.md` + `data/policies.json` + `data/health.json`（共 **6 文件**）

### 2.5 架构决策记录 (`docs/adr/`)
- ADR-001 技术栈选型 · ADR-002 数据模型与 JSON Schema · ADR-003 抓取框架与容错 · ADR-004 部署形态 · ADR-005 合规与数据审核

---

## 3. 测试通过数

```
$ ./.venv-d5/Scripts/python.exe -m pytest tests/ -v
==================== 50 passed, 2 subtests passed in 1.36s ====================
```

| 测试模块 | 数量 | 覆盖 |
|---|---|---|
| `tests/test_d3.py` | 3 | dedup / atomic_write / health 三件套 |
| `tests/test_d4.py` | 11 | integrated_fetch / 装饰器 / with-block 编排 / demo 离线无网络 |
| `tests/test_schemas.py` | 14 | Draft-07 加载 + 业务规则（id 模式、severity、score 边界、缺字段降级）|
| 其他集成用例 | 22 | utils / fetcher 端到端 |
| **合计** | **50 passed (+ 2 subtests)** | — |

---

## 4. 部署就绪状态

### 4.1 E2E 跑通结果（实测）

```
$ bash scripts/e2e.sh
[Stage 1] deploy_precheck → PASSED (21/21 checks, 0 fail, 0 warn)
[Stage 2] pytest → 50 passed, 2 subtests passed
[Stage 3] demo fetch → total=12 (log: demo 完成 ✅ +0 total=12 dup=6)
[Stage 4] deploy preview → 6 files 就绪
[Final] ✅ E2E PASSED
```

### 4.2 部署清单
- ✅ 环境：Python 3.11 + venv；依赖 aiohttp / bs4 / jsonschema / pytest 全部就位
- ✅ 数据：12 条 policies 通过 schema 校验
- ✅ 探针：2 个 fetcher 健康（healthy=True）
- ✅ 预览：`docs/preview/` 含完整 PWA 三件套 + 数据快照
- ✅ 日志：`logs/fetcher.log` 配置在模块导入早期创建（已修 P0-1）
- ✅ 集成：dedup → atomic → health 串接无脏写（test_atomic_write_no_partial_file 守护）

### 4.3 公网 PoC vs 内网
- 公网 Vercel 部署就绪（`docs/preview/index.html` 即可上传）
- 内网 Nginx 方案：IT 申请本周启动（D1 决策 #2）

---

## 5. 遗留 & 风险

| # | 项 | 等级 | 备注 |
|---|---|---|---|
| R1 | 仅 NDRC 一个真实源 | 中 | W2 接入能源局/工信部/网信办；基类已支持扩展 |
| R2 | 串行执行 fetcher | 低 | W2 改为 `asyncio.gather`；demo 链路已通 |
| R3 | 内网审批未启动 | 中 | IT 申请需 PM 跟进 |
| R4 | 合规审批人 = PM 暂代 | 低 | W2 招 1 名兼职（D1 决策 #4）|

---

## 6. W2 建议（Backend）

### P0 — 必做
1. **多源 fetcher**（P0-A）
   - 接入能源局 `miit`、工信部 `stats`，复用 `BaseFetcher`
   - 配置驱动（环境变量 `FETCHER_*_ENABLED`）已就绪，扩 2 个新源仅 ~40 LOC
2. **并发抓取**（P0-B）
   - `asyncio.gather` 替换串行；保留 single-flight 保护避免对同一站点并发
3. **真实 NDRC 抓取验证**（P0-C）
   - W1 走 demo 是为避 429；W2 在低峰时段（凌晨 03:00）试跑并比对真实 schema

### P1 — 应做
4. **数据校验层升级**
   - 增加 `validate_policies.py`：每日 cron 后对 `data/*.json` 全量 schema 校验，不合规即报警
5. **Git LFS 或去大文件**
   - `data/policies.json` 长期会超 1MB；提前考虑分片或外置对象存储
6. **CI 加强**
   - 在 `.github/workflows/daily-fetch.yml` 串接 `deploy_precheck.sh`；失败即禁 merge

### P2 — 待评估
7. **MongoDB / SQLite** 替换纯 JSON（取决于数据量）
8. **可视化 mock 数据准备** — 给 FE 提供与真实 schema 对齐的伪数据

---

## 7. 一句话总结

> W1 Backend 端到端链路（D1 决策 → D2 数据 → D3 容错 → D4 集成 → D5 E2E）全部跑通；
> 50 单测 + 21 项 deploy_precheck 全绿；`docs/preview/` 即时上线；
> **D1✓ D2✓ D3✓ D4✓ D5✓ W1 后端 DONE ✅**
