# W3 完结报告 — 5 源真抓稳定化 + 公网部署 + 推送 + 25 人内测

**项目**: NSP-IM 六网协同情报平台（内部代号 nsp-im）
**周期**: W3（2026-08-18 起，W1/W2 同日压缩迭代后的第三周冲刺）
**作者**: PM（软件经理）
**日期**: 2026-08-18
**状态**: 🔄 W3 收尾中 —— 内测已启动，Vercel 公网部署/Web Push/离线缓存并行收口

---

## 1. W3 五日完成情况

| 日 | 任务 ID | 主题 | 交付 | 状态 |
|---|---|---|---|---|
| D1 | t_cc758de9 | 5 源真抓稳定化 | `src/fetchers/robust.py`（UA/限流/反爬/重试）+ `scripts/_w3d1_real_fetch.py` + `logs/w3d1_real_fetch.log` | 🟡 部分（bs4 依赖缺失致发改委/能源局/南网 fallback，见 §4） |
| D2 | t_9a1e7918 | Vercel 公网部署 | 5 分钟 Vercel 公网 + 访问密码 + URL | 🔄 进行中（CLI 安装/部署，URL 未产出） |
| D3 | t_17cb4004 | Web Push 推送 | `src/utils/vapid.py` + `src/utils/webpush.py` + `data/.vapid.json`（VAPID 证书已生成）+ `scripts/_tmp_test_webpush.py` | 🟡 代码已落地，联调验证中 |
| D4 | t_ab28939a | 主屏引导 + 离线缓存 | `scripts/_w3d4_make_icons.py` + `scripts/_w3d4_subset_font.py` + `docs/preview/sw.js`（升级）+ `docs/preview/manifest.json` | 🟡 进行中 |
| **D5** | **t_064d9e83** | **25 人内测 + 反馈** | 内测方案 + 飞书群通知已发 + 反馈模板 + 本报告 | ✅ **内测已启动**（反馈回收 8/20 截止） |

```
W3 ▰▰▰▰▱ 80% (D5 内测进行中，D2 部署未闭环)
```

## 2. 关键产出清单

### 2.1 内测与反馈（本任务 W3-D5）
| 路径 | 类型 | 说明 |
|---|---|---|
| `docs/pm/W3-D5-内测方案.md` | 新增 | 内测目标/人群/入口/SOP/反馈渠道/风险兜底 |
| `docs/research/W3-内测反馈收集模板.md` | 新增 | 结构化反馈模板（设备/评分/问题/建议） |
| 飞书群通知 | 已发送 | 群 `oc_2d54c8a6…`，含内网入口 http://192.168.1.19:8080（message_id: om_x100b670822311030c4259de50bf3098） |
| 内网预览服务 | 已启动 | `docs/preview/` 静态站 + http.server :8080（后台 PID 26716），61 条数据同步 |

### 2.2 W3-D1 真抓稳定化
| 路径 | 说明 |
|---|---|
| `src/fetchers/robust.py` | 未提交（工作区）；UA 随机 + 限流 + 反爬规避 + 重试 |
| `scripts/_w3d1_real_fetch.py` | 5 源真抓驱动（与 W2-D5 同模式） |
| `logs/w3d1_summary.json` | 实测结果：**bs4 缺失 → 发改委/能源局/南网 fallback**（见 §4 风险） |

### 2.3 W3-D3 Web Push
| 路径 | 说明 |
|---|---|
| `src/utils/vapid.py` | VAPID 证书生成/加载 |
| `src/utils/webpush.py` | Web Push 推送实现 |
| `data/.vapid.json` | VAPID 公私钥（public_key 42B / private_key 32B，subject nspim@example.com） |

### 2.4 W3-D4 PWA 完善
| 路径 | 说明 |
|---|---|
| `scripts/_w3d4_make_icons.py` | PWA 图标生成 |
| `scripts/_w3d4_subset_font.py` | 字体本地化（去 Google Fonts，省 200-500ms） |
| `docs/preview/sw.js` | Service Worker 升级（离线缓存） |
| `docs/preview/manifest.json` | PWA manifest（可安装） |

## 3. 内测启动状态

| 项 | 状态 |
|---|---|
| 访问入口（内网） | ✅ http://192.168.1.19:8080（200 OK，61 条政策） |
| 访问入口（公网） | 🔄 Vercel 部署完成后追发 |
| 飞书群通知 | ✅ 已发（8/18） |
| 反馈截止 | ⏳ 8/20 18:00 |
| 有效反馈目标 | ⏳ ≥ 15 条（8/19 群内提醒一次） |

## 4. 风险与遗留项

| # | 风险/遗留 | 等级 | 应对 |
|---|---|---|---|
| 1 | **W3-D1 真抓 bs4 依赖缺失**：`ModuleNotFoundError: No module named 'bs4'` → 发改委/能源局/南网 fallback | 🟡 中 | 派 backend 装 bs4 重跑真抓；当前 preview 数据 61 条为 W2-D5 + 手动合成（demo 兜底），**内测不影响**（静态站可正常浏览） |
| 2 | **Vercel 公网 URL 未产出**（W3-D2 进行中） | 🟡 中 | 先用内网入口；URL 产出后由 W3-D2 任务追发通知 |
| 3 | Web Push 仅代码落地，端到端联调未验证 | 🟡 中 | W3-D3 任务收口时验证 |
| 4 | 内网 IP 192.168.1.19 可能随 DHCP 变化 | 🟢 低 | 通知注明「无法访问联系 PM 获取最新 IP」 |
| 5 | 反馈量不足 | 🟢 低 | 8/19 群内提醒；拉 wdw-guanjia 协同汇总 |

## 5. W4 建议（内测反馈汇总后输入）

1. 安装 bs4 依赖 → 真抓链路 5/5 全绿
2. Vercel 公网 URL 闭环 + 访问密码
3. Web Push 端到端验证（订阅 → 推送 → 展示）
4. 内测反馈汇总 → 分类归档 → 排期 W4 迭代
5. PWA 安装引导验收（iOS 添加到主屏 → 离线可用）

---
*— PM · W3-D5 · 2026-08-18*
